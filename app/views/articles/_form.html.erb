<%= simple_form_for(@article) do |f| %>
  <%= f.error_notification %>
  
    <%= f.input :title %>
    <%= f.cktext_area :body, :class => 'ckeditor'%>    
    <%= f.input :image, as: :file, label: "Cover picture"%>
    <%= f.button :submit %>
<% end %>
